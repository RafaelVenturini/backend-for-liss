export interface InsertPrint {
    est_id: string;
    name: string;
    tec_id: string;
    ref: string | number;
}

export interface InsertReference {
    sku: string;
    ref: string;
    name: string;
    price: number;
    category: string;
}